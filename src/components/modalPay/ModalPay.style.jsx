import styled from "styled-components";
import { blanco } from "../../styles/contantStyles";

export const ModalPayWrapper = styled.div`
  overflow: "scroll";
`;

export const PaperStyle = {
  backgroundColor: blanco,
  borderRadius: "16px",
  padding: "24px",
  overflow: "scroll",
  boxShadow: "2px 2px 4px 0 rgba(0, 0, 0, 0.2)",
};
